# sonar-rock-vs-mine-prediction-python-ML
This project builds a binary classification model to predict whether an object detected by sonar signals is a rock or a mine. It uses the Sonar dataset and applies Logistic Regression for training and prediction.
